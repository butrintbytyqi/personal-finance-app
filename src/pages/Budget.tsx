import React from 'react';
import { Typography, Box } from '@mui/material';

const Budget = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom fontWeight="bold" color="primary">
        Budget
      </Typography>
      {/* We'll implement the budget management interface here */}
    </Box>
  );
};

export default Budget;
