import React from 'react';
import { Typography, Box } from '@mui/material';

const Settings = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom fontWeight="bold" color="primary">
        Settings
      </Typography>
      {/* We'll implement the settings interface here */}
    </Box>
  );
};

export default Settings;
